server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://nozomi.proxy.rlwy.net:27508/railway?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: TqNpijROTHirzMlPGFwWjwiKNPFQnaHG
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html

dashscope:
  api-key: ${DASHSCOPE_API_KEY:sk-your-dashscope-api-key}
  model: qwen-plus
